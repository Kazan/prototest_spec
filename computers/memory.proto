syntax = "proto3";

package computers;

option go_package = ".;computers";

message Memory {
  enum Unit {
    UNKNOWN = 0;
    BYTE = 1;
    MEGABYTE = 2;
    TERABYTE = 3;
  }
  int32 value = 1;
  Unit unit = 2;
}